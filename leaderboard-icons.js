(()=>{const original=window.leaderboard;window.leaderboard=async function(c){const {data,error}=await db.from('players').select('*').order('wins',{ascending:false}).order('games_played',{ascending:false});if(error)throw error;const {data:profiles}=await db.from('profiles').select('id,avatar_url,display_name');c.innerHTML=`<section class="panel"><div class="panel-head"><h3>CHAMPIONS OF THE WAR</h3></div><div class="table leaderboard-with-icons">${(data||[]).map((p,i)=>{const pr=(profiles||[]).find(x=>x.id===p.user_id);return `<button class="rank-row player-rank-button" data-player-user="${esc(p.user_id)}"> <b>#${i+1}</b>${avatar({display_name:p.display_name,avatar_url:pr?.avatar_url},'leaderboard-avatar')}<strong>${esc(p.display_name)}</strong><span>${esc(p.primary_faction||'Unassigned')}</span><strong>${p.wins||0}W / ${p.losses||0}L / ${p.draws||0}D</strong><em>${rate(p)}%</em></button>`}).join('')||'<div class="empty">No ranked players yet.</div>'}</div></section>`;document.querySelectorAll('.player-rank-button').forEach(b=>b.onclick=()=>{if(typeof openPlayerDossier==='function')openPlayerDossier(b.dataset.playerUser);});};})();